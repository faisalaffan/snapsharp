services:
  reference-app:
    build:
      context: .
      dockerfile: src/SnapSharp.ReferenceApp/Dockerfile
    ports:
      - "8080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
